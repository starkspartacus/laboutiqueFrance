{% extends 'base.html.twig' %}

{% block title %}{{ produit.name }}{% endblock %}

{% block content %}

    <div class="row">
        <div class="col-md-5 mt-3">
            <img
              src="/uploads/{{ produit.illustration }}"
              alt="{{ produit.name }}"
              class="img-fluid"
            >
        </div>
        <div class="col-md-7">
            <h3>{{ produit.name }}</h3>
            <p>{{ produit.subtitle }}</p>
            <span class="font-weight-bold">{{ (produit.price/100) | number_format(2, ',','.') }} €</span>
            <hr>
            <p>
                {{ produit.description }}
            </p>
            <a href="#" class="btn btn-info">Ajouter au panier</a>
        </div>
    </div>


{% endblock %}